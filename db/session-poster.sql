-- Свой постер у сеанса.
-- Выполнить один раз в Neon SQL Editor.
--
-- У события постер обычно приходит из TMDB, но не всё, что показывают на
-- крыше, есть в TMDB: футбол, концерты, вечеринки, авторские программы.
-- Поле у сеанса перекрывает постер события; пустое — берётся событийный.
-- Так один и тот же фильм можно анонсировать разными афишами в разные дни.

ALTER TABLE sessions ADD COLUMN IF NOT EXISTS poster_url TEXT;
